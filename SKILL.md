---
name: minimal-paper-acrylic-cover
description: 把一张照片做成"极简纸感丙烯封面"——上半原照片、下半奶油米白纸底上的小幅手绘丙烯插画，配疏朗中英短句，艺术书/绘本质感。当用户要求"照片转封面/插画风/纸感丙烯/绘本风封面/上次那个风格处理照片"，或发来照片说"按这个风格做封面"时使用。支持批量处理一个文件夹，自动按横竖照片选择两种版面。
---

# 极简纸感丙烯封面

把单张照片转成 1080×1440（3:4）封面：上/原照片 + 奶油米白纸底上的小幅手绘丙烯插画 + 底部中英短句。

## 风格要点
- 纸底：带颗粒的粗糙奶油米白；插画为轻微不稳的纤细墨线 + 少量丙烯平涂色块（手工不规则毛边）。
- 主体偏小（下半区约15-20%），四周大量留白；配色取自原片，主色 ≤4 种。
- 禁止：写实/照片复刻、主体过大、3D、水彩晕染、纯线稿、多色、硬拼接满铺。
- 文字：中文宋体≈34px（深灰#3c3832），英文手写体≈30px（赭橙#b05a35），居中底部。

## 两种版面（按原片横竖自动）
- **横图（land）**：照片满宽贴顶占 680px（约47%），下方奶油纸底，插画缩小居中（≤820×340）并高斯羽化融入纸面，不硬拼接。
- **竖图（port）**：整幅奶油纸底，竖照片**完整不裁切**居中贴上部（≤860×800），下方居中放小插画（≤560×330）。

## 工作流程
1. **读图定方向**：用 `sips -g pixelWidth -g pixelHeight` 判断横竖；RAW（.CR3/.DNG/.NEF 等）先用 `sips -s format jpeg 原图 --out tmp.jpg` 转 JPEG。
2. **压缩并上传参考图**：把照片缩到 ≈1200px 宽，用文件上传工具得到 URL。
3. **生成插画**：用 image_edit（seedream_4.5），参考图=该 URL，输出 1080×720，提示词见 `references/prompt.md`。插画**不要带文字**。
4. **合成**：下载插画到本地，运行
   ```bash
      python3 scripts/compose.py --photo 原片.jpg --ill 插画.jpg --out 输出.jpg \
             --layout auto --cn "中文短句" --en "english title."
                ```
                   `--layout` 可显式 `land`/`port`；文字自拟短诗意中英对照（见 prompt.md 示例）。
                   5. **交付**：成品存到照片旁的 `风格成品/`；如用户要插画原稿，另存到 `插画原稿/`。
                   6. 抽查 1-2 张成品：照片未变形、插画未压字、纸底过渡自然。

                   批量：遍历文件夹每张重复 1-5，插图可并行生成。
   
